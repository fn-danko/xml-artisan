# TODO: migrate to Central Portal
# The `publish` task referenced here came from the old `maven-publish` block
# that pushed to GitHub Packages. That block has been removed as part of the
# migration to Maven Central via the vanniktech plugin. A replacement workflow
# will publish to the Sonatype Central Portal (tag-triggered, with GPG key and
# Portal credentials provided as secrets via ORG_GRADLE_PROJECT_* env vars).
# Disabled in the meantime so tag pushes don't fail CI.
#
# name: Publish to GitHub Packages
#
# on:
#   push:
#     tags:
#       - 'v*'
#
# jobs:
#   publish:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       packages: write
#     steps:
#       - uses: actions/checkout@v4
#
#       - uses: cachix/install-nix-action@v31
#         with:
#           nix_path: nixpkgs=channel:nixos-unstable
#
#       - name: Build and publish
#         run: nix develop --command ./gradlew publish
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

# GitHub Actions requires at least one valid job when the file exists, so we
# provide a minimal no-op to keep the workflow file syntactically valid.
name: Publish (disabled — migrating to Maven Central)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Publish workflow disabled pending Maven Central migration."
